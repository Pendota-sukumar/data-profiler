<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Results</title>
</head>
<body>
    <h1>Analysis Results</h1>

    <h2>Exploratory Data Analysis Insights</h2>
    <p><strong>Missing Values:</strong> {{ insights['missing_values'] }}</p>
    <p><strong>Data Types:</strong> {{ insights['incorrect_data_types'] }}</p>
    <p><strong>Skewed Features:</strong> {{ insights['skewed_features'] }}</p>
    <p><strong>High Correlation:</strong> {{ insights['high_correlation'] }}</p>
    <p><strong>Outliers:</strong> {{ insights['outliers'] }}</p>

    {% if class_report %}
        <h2>Classification Report</h2>
        <pre>{{ class_report }}</pre>
    {% endif %}

    {% if mse %}
        <h2>Regression Metrics</h2>
        <p>MSE: {{ mse }}</p>
        <p>R² Score: {{ r2 }}</p>
    {% endif %}

    <h2>Visualizations</h2>
    <img src="{{ url_for('static', filename=image_path1) }}" alt="Pairplot">
    
    {% if class_report %}
        <img src="{{ url_for('static', filename='images/confusion_matrix.png') }}" alt="Confusion Matrix">
    {% else %}
        <img src="{{ url_for('static', filename='images/actual_vs_predicted.png') }}" alt="Actual vs Predicted">
    {% endif %}

    <br><br>
    <a href="/">Back to upload page</a>
</body>
</html>
